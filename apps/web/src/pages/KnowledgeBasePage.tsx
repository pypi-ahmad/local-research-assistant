import { Surface } from "../components/Surface";

export function KnowledgeBasePage() {
  return (
    <Surface title="Knowledge Base" subtitle="Collections, tags, highlights, annotations, bookmarks">
      <p className="text-sm text-slate-300">
        Organize documents into folders and tagged collections. Highlights and annotations remain source-linked with full provenance.
      </p>
    </Surface>
  );
}
