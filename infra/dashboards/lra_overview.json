{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "type": "stat",
      "title": "API Requests",
      "targets": [{"expr": "sum(lra_requests_total)", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Retrieval Latency",
      "targets": [{"expr": "rate(lra_retrieval_latency_seconds_sum[5m])", "refId": "B"}],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    }
  ],
  "schemaVersion": 41,
  "title": "LRA Overview",
  "version": 1
}
